name: PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

concurrency:
  group: pr-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  deploy-preview:
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4

      - name: Install Surge
        run: npm install -g surge

      - name: Generate preview domain
        id: domain
        run: |
          HASH=$(echo -n "${{ github.repository }}" | md5sum | cut -c1-4)
          DOMAIN="sgtv-${HASH}-pr-${{ github.event.pull_request.number }}.surge.sh"
          echo "domain=$DOMAIN" >> "$GITHUB_OUTPUT"

      - name: Deploy to Surge
        run: surge ./ ${{ steps.domain.outputs.domain }} --token ${{ secrets.SURGE_TOKEN }}

      - name: Comment preview URL on PR
        uses: actions/github-script@v7
        with:
          script: |
            const previewUrl = `https://${{ steps.domain.outputs.domain }}`;
            const body = `🌿 **Preview deployed!**\n\n${previewUrl}\n\n_Updated for commit ${{ github.event.pull_request.head.sha }}_`;

            // Find existing bot comment to update instead of spamming
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('Preview deployed!')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  teardown-preview:
    if: github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Install Surge
        run: npm install -g surge

      - name: Generate preview domain
        id: domain
        run: |
          HASH=$(echo -n "${{ github.repository }}" | md5sum | cut -c1-4)
          DOMAIN="sgtv-${HASH}-pr-${{ github.event.pull_request.number }}.surge.sh"
          echo "domain=$DOMAIN" >> "$GITHUB_OUTPUT"

      - name: Teardown preview
        run: surge teardown ${{ steps.domain.outputs.domain }} --token ${{ secrets.SURGE_TOKEN }}
